//- The reason why he use pug,
//- He like to use Pug when working with Express, he think is better.
//- Pug is easier to code for people like to python because of indent and skipping code.

doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Hoom
        link(rel="stylesheet", href="https://unpkg.com/mvp.css@1.12/mvp.css")
    body
        header 
            h1 Hoom
        main
            div#myStream
                video#myFace(autoplay,playsinline, width="400", height="400")
                button#mute Mute
                button#camera Camera
            div#welcome
                form#enter
                    input#roomName(placeholder="room name", required, type="text")
                    input#nickName(placeholder="nickname", required, type="text")
                    button Enter Room
                h4 Open Rooms:
                ul
            div#room
                h3 
                ul
                form#name
                    input(placeholder="nickname", required, type="text")
                    button Change
                form#msg
                    input(placeholder="message", required, type="text")
                    button Send 
        script(src="/socket.io/socket.io.js")
        script(src="/public/js/app.js")